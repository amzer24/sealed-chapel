class_name Palette
extends RefCounted

## Shared colour language for the whole prototype: soot, bone, bruise-purple,
## dried-blood and curse-gold. Every placeholder sprite, prop and UI panel
## pulls from this palette so the kitbashed look stays consistent.

const SOOT := Color(0.071, 0.063, 0.086)
const SOOT_DARK := Color(0.035, 0.031, 0.043)
const BONE := Color(0.85, 0.80, 0.69)
const BONE_DIM := Color(0.62, 0.58, 0.51)
const BRUISE_PURPLE := Color(0.29, 0.20, 0.33)
const BRUISE_PURPLE_DARK := Color(0.17, 0.12, 0.20)
const DRIED_BLOOD := Color(0.42, 0.08, 0.10)
const DRIED_BLOOD_DARK := Color(0.24, 0.05, 0.06)
const CURSE_GOLD := Color(0.78, 0.62, 0.22)
const CURSE_GOLD_DIM := Color(0.55, 0.44, 0.18)
const FOG := Color(0.55, 0.55, 0.62, 0.30)
const IRON := Color(0.14, 0.14, 0.16)
const AMBIENT_TINT := Color(0.55, 0.50, 0.62)
