# Godot 4+ specific ignores
.godot/
.import/
export.cfg
export_presets.cfg
# Godot 4.4+ resource UID sidecars: regenerable per-editor, not committed
# anywhere else in this repo, so keep that consistent for new resources too.
*.uid

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/

# System / editor
.DS_Store
Thumbs.db
*.tmp
